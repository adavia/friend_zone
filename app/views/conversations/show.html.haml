.row
  .col-md-6.col-md-offset-3
    .wrapper
      .conversation-box
        %ul.conversation#messages-list{"data-conversation-id": "#{@conversation.id}"}
          = render @conversation.messages
      #conversation-form
        = render "messages/form", conversation: @conversation
